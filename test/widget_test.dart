import 'package:flutter_test/flutter_test.dart';

void main() {
  // Placeholder — no counter widget in LittleStarApp.
  // End-to-end UI tests live in integration_test/.
  testWidgets('smoke test placeholder', (WidgetTester tester) async {
    expect(true, isTrue);
  });
}
